import React from 'react';
import ReactDOM from 'react-dom/client';
import NasabahApp from './NasabahApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NasabahApp />
  </React.StrictMode>
);
